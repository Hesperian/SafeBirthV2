import { Permitted, PermittedAttribute, PermittedOrder } from './element';
import { DOMNode } from '../dom';
export declare class Validator {
    static validatePermitted(node: DOMNode, rules: Permitted): boolean;
    static validateOccurrences(node: DOMNode, rules: Permitted, numSiblings: number): boolean;
    static validateOrder(children: DOMNode[], rules: PermittedOrder, cb: (node: DOMNode, prev: DOMNode) => void): boolean;
    static validateAttribute(key: string, value: string | undefined, rules: PermittedAttribute): boolean;
    private static validatePermittedRule;
    private static validatePermittedCategory;
}
