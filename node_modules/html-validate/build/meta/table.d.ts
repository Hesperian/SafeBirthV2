import { DOMNode } from '../dom';
import { MetaElement, ElementTable } from './element';
export declare class MetaTable {
    readonly elements: ElementTable;
    constructor();
    init(): void;
    loadFromObject(obj: ElementTable): void;
    loadFromFile(filename: string): void;
    getMetaFor(tagName: string): MetaElement;
    private addEntry;
    private resolveGlobal;
    private mergeElement;
    resolve(node: DOMNode): void;
}
